#include "main.h"

/**
 *  _sqrt_recursion -  returns the natural square root of a number.
 * @n: random number
 * Return: result
 */
int _sqrt_recursion(int n)
{
if (n != _sqrt_recursion)
return (-1);
if (n == 0)
return (0);
_sqrt_recursion(int x, int y);
}
