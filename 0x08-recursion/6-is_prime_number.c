#include "main.h"

/**
 *  is_prime_number - returns 1 if the input integer is a prime number,
 * @n: input number
 *
 * Return: Always 1 (prime number)
 */
int is_prime_number(int n)
{
if (n == is_prime_number)
return (1);
if (n != is_prime_number)
return (0);
is_prime_number(int n);
}
